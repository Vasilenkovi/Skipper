<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkipperProject">
        


        <activity android:name=".MobilePackage.CommonUI.MainScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MobilePackage.CommonUI.LoginActivity"
            android:exported="true" />

        <activity android:name=".MobilePackage.CommonUI.CreateAccountActivity"
            android:exported="true" />

        <activity android:name=".MobilePackage.CommonUI.PersonalProfileActivity"
            android:exported="true" />


        <activity android:name=".MobilePackage.CommonUI.Tools.TestActivity"
            android:exported="true" />
        <activity android:name=".MobilePackage.CommonUI.MentorInterface.AccountActivity"/>

        <activity android:name=".MobilePackage.CommonUI.MentorInterface.ChatsActivity"/>
    </application>

</manifest>
